# Nudge OS — runtime dependencies (Windows, Python 3.13)
# Pinned-compatible: exact pin for Qt (ABI-sensitive), floors for the rest.
PyQt5==5.15.11
anthropic>=0.40
mss>=9.0
Pillow>=10.0
uiautomation>=2.0
comtypes>=1.2
pynput>=1.7
pyttsx3>=2.90
# Only needed to run the MCP server (nudge_mcp.py); the desktop app + HTTP
# control API have no extra deps (the control server is stdlib http.server).
mcp>=1.2
